{% extends "admin/base.html.twig" %}

{% block title %}{{ parent() }} Formulaire de {% if business != null %} {{ business.name }} {% endif %}commerce{% endblock %}

{% block body %}
    <div class="container">
        <h2>FORMULAIRE DE COMMERCE</h2>
        {% if business != null %}
            <h3>{{ business.name }}</h3>
        {% endif %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
            <button type="submit" class="btn btn-success mr-4"><i class="fas fa-check mr-2"></i>Confirmer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}