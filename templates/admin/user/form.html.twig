{% extends "admin/base.html.twig" %}

{% block title %}{{ parent() }} Formulaire de {{ user.firstname }} {{ user.lastname }}{% endblock %}

{% block body %}
    <div class="container">
        <h2>FORMULAIRE D'UTILISATEUR</h2>
        <h3>{{ user.firstname }} {{ user.lastname }}</h3>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success mr-4"><i class="fas fa-check mr-2"></i>Confirmer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}